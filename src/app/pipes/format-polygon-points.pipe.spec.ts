import { FormatPolygonPointsPipe } from './format-polygon-points.pipe';

describe('FormatPolygonPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPolygonPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
