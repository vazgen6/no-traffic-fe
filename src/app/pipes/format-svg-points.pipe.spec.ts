import { FormatSvgPointsPipe } from './format-svg-points.pipe';

describe('FormatSvgPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSvgPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
